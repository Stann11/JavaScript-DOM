//get multiple elements by get elements by class name
//get multiple elements item using queryselectorall

const a = document.getElementsByClassName("nav-items"); //HTML Collection is obtained which is type of object
console.log(a);

const b = document.querySelectorAll(".nav-items"); //Node list is obtained which is type of object 
console.log(b);